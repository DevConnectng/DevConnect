# DevConnect

A developer networking platform for Nigerian developers with gig posting, collaboration, and direct messaging.

## Features
- User authentication with JWT
- Gig posting and browsing
- Threaded comments
- Private messaging
- Real-time notifications
- Admin moderation panel
- Responsive dark-mode design

## Setup
1. Clone repository
2. Run `npm install`
3. Create `.env` file from `.env.example`
4. Run `npm start` or `npm run dev`

## Tech Stack
- Frontend: Vanilla HTML, Tailwind CSS, JavaScript
- Backend: Node.js, Express
- Database: SQLite
- Authentication: JWT + bcrypt

## API Documentation
See `src/routes/` for API endpoints
